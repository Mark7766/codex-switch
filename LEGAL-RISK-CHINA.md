# Codex Switch 中国法律风险评估报告

> **评估日期：** 2026-06-24  
> **评估范围：** Codex Switch v1.16.0 完整代码库  
> **评估方法：** 静态代码分析 + 架构审查  
> **适用法律：** 《中华人民共和国网络安全法》《中华人民共和国数据安全法》《中华人民共和国个人信息保护法》《生成式人工智能服务管理暂行办法》及相关法规

---

## 风险等级总览

| 等级    | 数量 | 说明                               |
| ------- | ---- | ---------------------------------- |
| 🔴 严重 | 2    | 存在较高法律合规风险，建议尽快处理 |
| 🟠 高   | 3    | 存在明显合规缺口，建议近期处理     |
| 🟡 中   | 4    | 存在潜在风险，建议逐步完善         |
| 🟢 低   | 3    | 影响有限，可酌情处理               |

---

## 🔴 严重风险

### 风险 1：代理服务可能触及电信业务准入监管

**涉及法律：** 《中华人民共和国电信条例》第七条、第九条；《电信业务经营许可管理办法》

**现状描述：**

Codex Switch 的核心功能是在用户本地启动一个 HTTP/WebSocket 代理服务（默认 `127.0.0.1:11435`），实时拦截并转发 AI API 流量。该代理将 Codex CLI/Desktop 发出的请求（OpenAI Responses API / Anthropic Messages API 格式）转换为 DeepSeek / Agnes AI / Zhipu GLM 等供应商的 API 格式后发出。

**风险分析：**

虽然代理仅绑定 `127.0.0.1`（本地回环地址），但从功能实质上看：

- 它是一个**中间网络节点**，对通信内容进行协议解析和转换
- 它在用户不知情的情况下**修改了网络请求的目标地址和内容格式**
- 如果用户在境内使用，将境内 AI 服务请求转发至境外服务（如 Agnes AI），可能触及跨境数据流动监管

**潜在后果：**

- 若被认定为"经营电信业务"而未取得相应许可，依据《电信条例》第六十九条，可能面临**责令改正、没收违法所得、处违法所得 3-5 倍罚款**；情节严重的，可能被责令停业整顿
- 若被认定为"非法提供 VPN 翻墙服务"（尽管目前仅监听本地），将触及更严重的法律红线

**建议措施：**

1. **优先**：在应用界面和文档中明确声明本工具为"本地开发辅助工具"，仅用于开发者提高工作效率，不具备网络代理/VPN 功能，不提供跨境网络接入服务
2. **优先**：对境外供应商（Agnes AI 等）增加明确的合规提示，告知用户跨境数据传输的法律风险
3. **建议**：咨询专业律师，确认本地回环代理是否属于电信业务准入豁免范围
4. **建议**：默认供应商仅保留境内合规供应商（DeepSeek、GLM），境外供应商作为"高级/开发者选项"需用户主动确认知情同意
5. **建议**：在用户首次选择境外供应商时，弹出明确的法律风险告知对话框

---

### 风险 2：AI 代理转接服务的合规定性不明确

**涉及法律：** 《生成式人工智能服务管理暂行办法》第二条、第十七条

**现状描述：**

Codex Switch 将用户通过 Codex/Claude 客户端发起的 AI 对话请求，经过协议转换后转发给 DeepSeek 等 AI 服务商。用户在 Codex/Claude 客户端中看到的是"OpenAI/Anthropic 的模型"，但实际处理请求的是 DeepSeek/Agnes AI 等第三方。

**风险分析：**

- 根据《生成式人工智能服务管理暂行办法》，向境内公众提供生成式 AI 服务的，应当向国家网信部门**履行算法备案手续**，并**进行安全评估**
- 虽然 Codex Switch 自身不训练/运行模型，但其"中间人"角色使得**模型来源信息不透明**——用户可能误以为自己在使用 OpenAI 的模型，而实际服务由 DeepSeek 提供
- 如果用户通过 Codex Switch 生成的 AI 内容违反《生成式人工智能服务管理暂行办法》第四条（如违法信息、歧视性内容），责任归属存在争议
- DeepSeek 的 API 使用协议可能限制第三方中转/转售，Codex Switch 的中转行为可能违反 DeepSeek 的服务条款

**建议措施：**

1. **优先**：在主界面 100% 透明地展示当前实际使用的 AI 供应商和模型名称，不允许任何隐藏或混淆
2. **优先**：在应用中增加明确的免责声明："本工具仅为协议转换工具，不提供 AI 模型服务。实际 AI 服务由所选供应商独立提供，其生成内容的合法性由供应商和用户自行负责"
3. **建议**：审查 DeepSeek、Agnes AI、GLM 等各供应商的 API 使用协议，确认中转/转发行为是否被允许
4. **建议**：建立内容安全过滤的应急预案——如果上游供应商返回违法内容，代理层应有拦截能力
5. **建议**：关注《生成式人工智能服务管理暂行办法》配套细则的发展，如果未来将"AI 代理/网关"纳入监管范围，应第一时间响应

---

## 🟠 高风险

### 风险 3：遥测数据默认开启收集，违反 PIPL 知情同意原则

**涉及法律：** 《个人信息保护法》第十三条、第十四条、第十七条

**现状描述：**

代码位于 `electron/server-client/telemetry.ts`。Telemetry 系统**默认开启**（`telemetryEnabled` 默认值为 `true`），在用户未进行任何确认的情况下，应用启动时即开始收集数据并上传至 `codex-switch.cloud`。

**收集的数据包括：**

| 事件类型       | 收集内容                                                         |           是否含个人信息            |
| -------------- | ---------------------------------------------------------------- | :---------------------------------: |
| `app_start`    | client_id, app_version, platform, arch, os_version, 是否首次运行 |           是（设备标识）            |
| `app_close`    | 运行时长, 请求次数                                               |                 否                  |
| `proxy_start`  | 端口, 默认模型                                                   |                 否                  |
| `proxy_error`  | 错误类型, 端口, 错误信息                                         |          可能（错误信息）           |
| `error`        | 错误类型, 错误消息, 错误堆栈(前500字符), 平台, 版本              | **是**（堆栈可能含文件路径/用户名） |
| `model_call`   | 聚合计数（5分钟窗口）, 不含具体对话内容                          |                 否                  |
| `config_write` | 配置变更字段名                                                   |                 否                  |
| `update_check` | 当前版本, 是否有更新, 镜像模式                                   |                 否                  |
| `smart_search` | 查询长度, 耗时, 是否成功                                         |                可能                 |

**风险分析：**

- **PIPL 第十三条**要求处理个人信息应当取得个人同意（知情、自愿、明确）。默认开启遥测并传输 `client_id`（持久化设备标识符）和错误堆栈（可能包含用户名、文件路径），**未取得用户的事先同意**
- **PIPL 第十七条**要求个人信息处理者以显著方式、清晰易懂的语言告知处理目的、方式、种类、保存期限等。当前仓库内**不存在任何隐私政策文档**
- 错误堆栈追踪 (`error.stack?.slice(0, 500)`) 可能暴露：
  - 用户的文件系统路径（含用户名，如 `/Users/zhangsan/...`）
  - 第三方依赖的版本信息
  - 代码执行上下文
- `client_id` 是一个持久化的 16 位十六进制字符串，可跨会话追踪用户行为，属于**可关联到特定设备**的标识符

**建议措施：**

1. **必须**：将遥测从默认开启（opt-out）改为默认关闭，在首次启动向导中增设"体验优化计划"页，让用户明确选择是否加入（opt-in）
2. **必须**：撰写出隐私政策文档，嵌入应用（设置页 + 首次启动页）和仓库 README，说明：
   - 收集哪些数据
   - 数据用途
   - 数据存储位置（codex-switch.cloud 服务器所在地区）
   - 数据保留期限
   - 用户权利（查看、删除、撤回同意）
   - 联系方式
3. **必须**：对错误堆栈进行更强的脱敏处理——移除文件系统路径、用户名等 PII
4. **建议**：提供"仅本地使用"模式，完全不连接任何远程服务（包括遥测、更新检查、插件下载）
5. **建议**：评估 `codex-switch.cloud` 服务器是否部署在中国境内。若在境外，还需额外满足 PIPL 第三章"个人信息跨境提供规则"

---

### 风险 4：数据传输至 codex-switch.cloud 缺乏透明度和合规准备

**涉及法律：** 《个人信息保护法》第三十八条、第三十九条；《数据安全法》第二十一条

**现状描述：**

应用默认向 `https://www.codex-switch.cloud/api/v1` 发送数据，包括遥测数据、更新检查、社区功能数据（邀请码、推荐计数）、智能搜索查询。该服务器 URL 硬编码在 `electron/server-client/config.ts` 中。

**风险分析：**

- 服务器部署位置不明（境内/境外）。若在境外，数据传输出境需满足 PIPL 第三十八条的跨境传输条件（如通过国家网信部门安全评估、获得个人信息保护认证、或签订标准合同）
- 用户不知道自己的数据被发送到了哪里、由谁控制
- 即使遥测被关闭，更新检查仍会发送 `current_version` 到服务器
- 没有发现数据处理协议（DPA）或服务等级协议（SLA）

**建议措施：**

1. **优先**：确认并公开 codex-switch.cloud 服务器的物理部署位置和运营主体
2. **优先**：如果服务器在境外，且项目有境内用户，应当：
   - 提供境内用户使用的替代服务器（或将服务器迁移至境内）
   - 在隐私政策中明确告知数据出境情况
   - 评估是否触发 PIPL 跨境传输安全评估门槛
3. **建议**：将更新检查改为仅连接 GitHub Releases（公开的、可审计的），或者提供独立的仅本地使用版本
4. **建议**：允许用户在设置中将服务器 URL 设为空，彻底禁用所有远程通信

---

### 风险 5：软件著作权与许可证合规

**涉及法律：** 《中华人民共和国著作权法》；《计算机软件保护条例》

**现状描述：**

- `package.json` 中声明 `"license": "MIT"`
- README.md 中有 MIT 徽章
- 但**仓库根目录缺少 `LICENSE` 文件**，MIT 许可证的完整文本不存在

**风险分析：**

- 根据开源社区惯例和法律实践，仅声明 MIT 而不附带完整许可证文本，可能导致许可证效力受到质疑
- MIT 许可证要求"上述版权声明和本许可声明应包含在本软件的所有副本或实质性部分中"——如果项目本身都没有完整的 LICENSE 文本，下游使用者无法满足此要求
- **对下游使用者**：缺乏明确、完整的许可证文件可能导致企业用户（尤其是有合规要求的境内企业）无法使用该软件

**建议措施：**

1. **必须**：在仓库根目录添加标准 `LICENSE` 文件，包含完整的 MIT 许可证文本和版权声明
2. **建议**：在 `electron-builder.yml` 打包配置中，确保 `LICENSE` 文件被打包进安装包
3. **建议**：在 `README.md` 中增加 License 章节，说明许可证类型

---

## 🟡 中风险

### 风险 6：配置文件修改的授权与安全性

**涉及法律：** 《网络安全法》第二十一条（网络安全等级保护）；《个人信息保护法》第五十一条（安全技术措施）

**现状描述：**

应用会自动修改用户机器上的多个配置文件（详见 `electron/codex/`）：

- `~/.codex/config.toml` 和 `auth.json`（mode 0600）
- `~/.claude/settings.json`、`config.json`
- `~/Library/Application Support/Claude/claude_desktop_config.json`
- `~/Library/Application Support/Claude-3p/` 下的多个配置文件
- Shell 配置文件（`~/.zshrc`、`~/.bash_profile` 等）中的环境变量注入

**风险分析：**

- 修改 Shell 配置文件（`.zshrc`、`.bash_profile`）尤为敏感——如果管理不当，可能注入恶意环境变量或被恶意利用
- 虽然有备份机制（`.bak.<timestamp>`）和 `__codexSwitch: "managed"` 标记管理，但如果标记逻辑出现 bug，可能覆盖用户的手动配置
- 修改 `auth.json` 时虽然设置了 `0o600` 权限，但写入的内容（API Key）如果被其他本地进程读取，仍存在泄露风险
- 配置注入本质上是一种**本地提权操作**（虽然不是传统安全意义上的），应该让用户充分知晓

**建议措施：**

1. **建议**：在首次写入配置前，弹出确认对话框，**逐一列明**将要修改的文件清单及修改内容
2. **建议**：为配置文件操作增加完整性校验——写入前计算原始文件 hash，还原时验证
3. **建议**：增加"仅查看配置差异"功能，让用户了解当前配置与实际需求的差异
4. **建议**：Shell 注入的 `managed block` 使用更明显的方式标记（如彩色注释），便于用户识别

---

### 风险 7：API Key 本地存储安全

**涉及法律：** 《个人信息保护法》第五十一条；《数据安全法》第二十七条

**现状描述：**

- 主要方案：使用 `keytar` 通过操作系统原生密钥链存储 API Key（macOS Keychain / Windows Credential Manager）
- 降级方案：若 keytar 不可用，使用 `electron-store` 加密存储（硬编码密钥 `'codex-switch-local-only'`）

**风险分析：**

- 降级方案的加密密钥是硬编码在源码中的固定字符串，**不安全**——任何能读取本地文件的人都能解密
- API Key 在代理内存中以明文形式存在（因为需要用于上游请求认证），这是功能必要性，但应确保内存中不留残留
- API Key 虽然没有传输到 codex-switch.cloud（代码中有 `sanitizeProperties` 脱敏），但若遥测中意外包含（bug 导致），存在泄露风险

**建议措施：**

1. **建议**：降级方案至少使用机器级特征（如 machine-id 派生密钥）替代硬编码密钥
2. **建议**：在 telemetry 脱敏逻辑中增加自动化测试，确保任何情况下 API Key 都不会被上传
3. **建议**：增加"清除所有敏感数据"功能，允许用户一键清除所有本地存储的 API Key 和缓存

---

### 风险 8：依赖供应链合规

**涉及法律：** 《网络安全法》第二十二条（网络产品和服务安全）；《网络安全审查办法》

**现状描述：**

项目使用的主要依赖均为 npm 生态中的 MIT/Apache-2.0 许可证包，包括 Electron、React、Vite、Tailwind CSS 等。未使用任何 GPL/AGPL 等强传染性许可证的依赖。网络请求全部使用 Node.js 原生 `http`/`https` 模块，无第三方 HTTP 客户端依赖。

**风险分析：**

- 从当前直接依赖看，许可证合规状况良好，无"传染性"许可证风险
- 但 npm 生态的**间接依赖链**很长，可能存在未知的许可证合规问题或安全漏洞
- Electron 内嵌 Chromium，Chromium 本身涉及大量第三方代码和许可证
- 未发现使用 `npm audit` 或 SBOM 生成工具的配置

**建议措施：**

1. **建议**：运行 `npm audit` 或 `pnpm audit` 检查已知安全漏洞
2. **建议**：生成软件物料清单（SBOM），便于企业用户进行供应链审查
3. **建议**：在 CI pipeline 中加入许可证检查和依赖审计
4. **建议**：定期审查关键依赖的更新和安全公告

---

### 风险 9：软件分发与代码签名

**涉及法律：** 《网络安全法》第二十二条（网络产品安全漏洞管理）

**现状描述：**

- macOS 构建包：`identity: null`、`sign: false` → **未签名**
- Windows 构建包：NSIS 安装器 → **未签名**
- 分发渠道：GitHub Releases + codex-switch.cloud 更新服务 + GHProxy 镜像

**风险分析：**

- 未签名的软件包在 macOS 上会触发 Gatekeeper 警告，在 Windows 上会触发 SmartScreen 警告
- 如果用户的设备因运行未签名软件出现问题，责任归属可能产生争议
- GitHub 在中国大陆的访问不稳定，通过 GHProxy 镜像分发可能涉及额外的法律风险（如未经授权的内容镜像/分发）
- 未签名的安装包容易被第三方篡改后重新分发，可能导致用户下载到恶意版本

**建议措施：**

1. **建议**：macOS 申请 Apple Developer Program 并进行代码签名和公证（notarization）——这对有境内 Mac 用户的项目提升可信度很重要
2. **建议**：Windows 考虑获取代码签名证书（OV 或 EV）
3. **建议**：若无法获得签名证书，在下载页面显著位置提示用户校验 SHA256/MD5 摘要
4. **建议**：如果 GHProxy 是第三方镜像服务，确认其服务条款允许该使用方式，或自建镜像

---

## 🟢 低风险

### 风险 10：项目不属于关键信息基础设施（CII）

**现状分析：**

Codex Switch 是一个个人开发者维护的开源桌面工具，不是《关键信息基础设施安全保护条例》定义的关键信息基础设施（CII）运营者。不涉及重要行业（金融、能源、交通、公共服务等）。

**建议：** 无需特殊处理。但若未来向企业级 SaaS 方向发展，需重新评估。

---

### 风险 11：不涉及数据出境安全评估申报触发门槛

**现状分析：**

根据《数据出境安全评估办法》第四条，处理 100 万人以上个人信息的数据处理者向境外提供个人信息，或自上年 1 月 1 日起累计向境外提供 10 万人以上个人信息或 1 万人以上敏感个人信息的数据处理者向境外提供个人信息，才需要申报安全评估。

Codex Switch 作为一个桌面端本地工具，用户规模远未达到 100 万人门槛，且传输至 codex-switch.cloud 的数据量相对有限（不含敏感内容）。

**建议：** 监控用户规模。若日活用户超过数万级，应提前准备数据出境合规方案（参考风险 4 的建议）。

---

### 风险 12：加密技术使用合规

**涉及法律：** 《商用密码管理条例》

**现状分析：**

应用使用 TLS/SSL 进行上游 HTTPS 通信（标准系统库），使用硬编码密钥进行本地 electron-store 加密。这些都是常规商用密码应用场景，不涉及商用密码进出口或国家秘密。

**建议：** 无需特殊处理。

---

## 合规行动检查清单

按优先级排列的整改清单：

### 立即处理（1-2 周内）

- [ ] **隐私政策**：撰写并发布隐私政策文档（`docs/PRIVACY-POLICY.md`），嵌入应用和仓库 README
- [ ] **遥测 Opt-in**：将 Telemetry 改为默认关闭，在首次启动向导中增加 opt-in 页面
- [ ] **LICENSE 文件**：在仓库根目录添加完整 MIT 许可证文本
- [ ] **错误堆栈脱敏**：移除遥测上报的错误堆栈中的文件路径和用户名

### 近期处理（1 个月内）

- [ ] **法律咨询**：咨询中国网络安全/数据合规律师，确认本地代理的法律定性
- [ ] **供应商合规提示**：对境外供应商增加跨境数据传输风险提示
- [ ] **供应商协议审查**：审查 DeepSeek / Agnes AI / GLM 的 API 使用条款
- [ ] **服务器信息公开**：公开 codex-switch.cloud 的部署位置和运营主体
- [ ] **免责声明**：在应用主界面和文档中增加明确的工具定位声明

### 中长期（3 个月内）

- [ ] **代码签名**：获取 Apple Developer 证书和 Windows 代码签名证书
- [ ] **SBOM**：生成软件物料清单
- [ ] **安全审计**：对 proxy 通信安全、API Key 存储安全进行独立安全审计
- [ ] **独立版本**：提供完全离线、不连接任何远程服务的"纯本地版"
- [ ] **依赖审计**：建立自动化依赖安全审计 CI
- [ ] **内容安全预案**：制定上游 AI 内容安全问题的应急预案
- [ ] **配置修改透明化**：增加配置文件修改前的用户确认对话框

---

## 免责声明

本报告基于对 Codex Switch v1.16.0 代码库的静态分析生成，仅作为技术层面的合规风险提示，**不构成法律意见**。

中国网络安全和数据保护领域的法律法规正在快速发展中，包括《生成式人工智能服务管理暂行办法》的配套细则、数据出境安全管理的新规等。建议项目维护者：

1. **咨询持有中国律师执业资格的专业人士**获取正式法律意见
2. **持续关注**网信办、工信部、公安部等主管部门的最新法规和指引
3. **定期更新**本风险评估报告，最好在每次重大版本发布前复核

---

> **报告生成于：** 2026-06-24  
> **下一步建议：** 优先处理 "🔴 严重风险" 中的两项，尤其是代理服务法律定性和 AI 中转服务合规性，建议在咨询专业律师后形成正式的合规方案。
